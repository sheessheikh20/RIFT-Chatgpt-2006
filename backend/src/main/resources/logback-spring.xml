<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ╔══════════════════════════════════════════════════════╗ -->
    <!-- ║  ChatGPT Professional Enterprise Suite — Logback    ║ -->
    <!-- ╚══════════════════════════════════════════════════════╝ -->

    <!-- Application data base path from Spring property -->
    <springProperty name="APP_BASE_PATH" source="app.data.base-path"
                    defaultValue="${user.home}/ChatGPT Professional"/>

    <!-- ─── Console Appender ─────────────────────────────────── -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ─── Rolling File Appender ────────────────────────────── -->
    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${APP_BASE_PATH}/Logs/spring.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Roll over daily, keep 30 days -->
            <fileNamePattern>${APP_BASE_PATH}/Logs/spring.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy
                    class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%level] [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ─── Log Level Configuration ──────────────────────────── -->

    <!-- Our application: full INFO logging -->
    <logger name="com.openai.chat2006" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ROLLING_FILE"/>
    </logger>

    <!-- Hibernate SQL: only warnings (enable DEBUG to see SQL) -->
    <logger name="org.hibernate" level="WARN"/>
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="org.hibernate.type.descriptor.sql" level="WARN"/>

    <!-- Spring framework: warnings only -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.springframework.security" level="WARN"/>
    <logger name="org.springframework.web" level="WARN"/>

    <!-- Apache HTTP client (used by RestTemplate) -->
    <logger name="org.apache.http" level="WARN"/>

    <!-- Root logger: INFO to console, WARN to file -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
